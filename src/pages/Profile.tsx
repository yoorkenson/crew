import React, { FC } from 'react';

const Profile: FC = () => {
    const style = {
        color: '#F36F3B',
        fontSize: '40px',
        marginTop: '50vh'
    }
    return (
        <h1 style={style}>
            PROFILE PAGE
        </h1>
    );
};

export default Profile;